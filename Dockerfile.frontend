# Dockerfile para Frontend - Railway
FROM nginx:alpine

# Copiar archivo HTML al directorio de nginx
COPY frontend-functional.html /usr/share/nginx/html/index.html

# Copiar configuración personalizada de nginx si es necesaria
COPY nginx.conf /etc/nginx/nginx.conf

# Exponer puerto 80
EXPOSE 80

# Comando por defecto
CMD ["nginx", "-g", "daemon off;"]